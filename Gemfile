# frozen_string_literal: true

source 'https://rubygems.org'

# For testing with ActiveAdmin 4.x beta (using esbuild + Tailwind, not importmap)
gem 'activeadmin', '~> 4.0.0.beta16'

group :test do
  gem 'pry-byebug'
  gem 'puma'
  gem 'propshaft'
end

gemspec
