# frozen_string_literal: true

source 'https://rubygems.org'

# For testing with ActiveAdmin 4.x beta
gem 'activeadmin', '~> 4.0.0.beta16'

group :test do
  gem 'importmap-rails', '>= 2.0'  # Required by ActiveAdmin 4
  gem 'propshaft'
  gem 'pry-byebug'
  gem 'puma'
end

gemspec
